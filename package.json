{
  "name": "hashstrat-pool",
  "version": "0.0.1",
  "description": "Pool contracts of the  HashStratp protocol",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "clean": "npx hardhat clean",
    "chain": "npx hardhat node",
    "deploy:goerli": "npx hardhat run --network goerli scripts/deploy-goerli.ts",
    "deploy:polygon": "npx hardhat run --network polygon scripts/deploy-polygon.ts",
    "test": "npx hardhat test",
    "run-test-script": "npx hardhat run --network polygon scripts/get-tx-count.ts"
  },
  "keywords": [
    "solidity",
    "hardhat"
  ],
  "author": "cpascoli",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.1",
    "@nomiclabs/hardhat-etherscan": "^3.1.5",
    "hardhat": "^2.12.6",
    "hardhat-contract-sizer": "^2.7.0",
    "hardhat-deploy": "^0.11.22"
  },
  "dependencies": {
    "@chainlink/contracts": "^0.6.0",
    "@nomicfoundation/hardhat-network-helpers": "^1.0.7",
    "@openzeppelin/contracts": "^4.8.1",
    "dotenv": "^16.0.3",
    "ethers": "^5.7.2",
    "hardhat-abi-exporter": "^2.10.1"
  }
}
